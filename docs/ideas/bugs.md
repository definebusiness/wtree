# Bugs and inconsistencies

# Project manifest should be checked in

README.md instructs to `git add .gitignore project.wtree.yml`. That is totally wrong. This file needs to be pushed in order to be able to fully clone a nested repo.

