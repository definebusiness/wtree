# Run ledger — `wtree` incremental implementation plan

## Run state

- Plan: [`docs/plans/wtree-implementation-plan.md`](../../plans/wtree-implementation-plan.md)
- State: `complete`
- Started: historical start predates this durable ledger
- Last updated: 2026-08-12
- Active milestone / phase: `M18 — Final independent acceptance review` / `approved`
- Resume from: none; terminal acceptance is approved.
- Final response permitted: `yes`

## Current milestone ledger

- Scope checklist:
  - [x] Clean-room specification review of all required commands/options/contracts/invariants.
  - [x] Run static/unit/integration/race/packaging/help/E2E acceptance gates.
  - [x] Verify cross-platform CI evidence and documentation accuracy.
  - [x] Ensure complete traceability §§1–93 without required MVP deferrals.
  - [x] Remediate all material findings test-first and re-review to approval.
  - [x] Meet M18 exit criteria: release-ready independent approval/repository gate.
  - [x] Meet applicable global DoD: RED→GREEN→REFACTOR evidence; hermetic tests;
    focused tests, `go test ./...`, `go test -race ./...`, `go vet ./...`,
    formatting, and `make check`; contextual errors and stable contracts.
- Implementation tier: `normal`
- Remediation attempts: `1`
- Finding set:
  - R1 — resolved, medium: inspection/dry-run CLI families use mutating resolver reconciliation.
  - R2 — resolved, medium: JSON error code classification diverges from numeric exit classification.
  - R3 — resolved, medium: inherited `--project` on init is silently ignored.
  - R4 — unresolved, medium: command help still advertises inherited `--project` semantics that init rejects and config overloads.
  - R5 — resolved, medium: traceability is broad M17 draft, not accurate §§1–93 audit.
- Current packet and evidence: clean-room reviewer approved the complete normal-tier R4 remediation; main-agent terminal verification passed. The complete R1–R5 remediation was rejected once; all R1–R5 are resolved.
- Verification / adjudication: M17 approved after normal independent review and main-agent verification.

## Historical bootstrap

M00 through M04 are approved according to the append-only execution log in the
source plan. Their detailed action-level run evidence predates this ledger and
is not reconstructed here. This ledger is authoritative for checkpoints from
M05 onward.

## Action log

| Time | Actor | Action | Outcome / evidence | Resulting state and resume instruction |
|---|---|---|---|---|
| 2026-08-12 | main agent | Initialized durable run ledger from the source-plan execution log. | M00–M04 are recorded approved; M05 is the first unchecked milestone. | `active`, M05 `implementing`; create checklist and dispatch `implementer`. |
| 2026-08-12 | main agent | Resumed interrupted plan run and reconciled the worktree. | Read the plan, M05 specification, ledger, current resolver/domain/store code, and tests. The worktree contains unrecorded changes (including staged-delete/untracked replacement pairs for store/test helpers); no prior M05 implementer/reviewer evidence exists, so no completion or finding decision is inferred. | `active`, M05 `implementing`; dispatch the complete initial M05 packet to `implementer`. |
| 2026-08-12 | main agent | Created M05 scope checklist and working plan. | Checklist covers all source-plan scope, named test slices, exit criterion, and applicable global verification gates. | `active`, M05 `implementing`; dispatch the complete initial M05 packet to `implementer`. |
| 2026-08-12 | main agent | Dispatched initial M05 packet to `implementer`. | Complete test-first packet assigns resolver implementation/tests and requires RED/GREEN evidence for all M05 scope, focused and global quality gates. | `active`, M05 `implementing`; await a complete implementation submission, then inspect evidence before review. |
| 2026-08-12 | implementer | Submitted complete initial M05 implementation. | Added local-config precedence test and resolver changes. Reported RED from `go test ./internal/service -run '^TestResolverPrefersLocalConfigOverPersistedWorkspaceRegistry$' -count=1`; GREEN after change; reported focused resolver tests, `go test ./...`, race, vet, `make check`, gofmt, and diff check all passing. | `active`, M05 `implementing`; main agent inspected focused resolver suite and diff check successfully, then sends current filesystem to independent review. |
| 2026-08-12 | main agent | Dispatched M05 independent review to `reviewer`. | Reviewer packet requires read-only inspection of the current shared filesystem, full M05/spec review, focused and global verification, and severity/location-backed findings. | `active`, M05 `reviewing`; await reviewer decision. |
| 2026-08-12 | reviewer | Reviewed complete M05 submission. | APPROVED, no material findings; focused resolver tests, full tests/race/vet/format/diff checks, and `make check` passed. | `active`, M05 `reviewing`; main agent runs required verification and records approval if it passes. |
| 2026-08-12 | main agent | Verified and approved M05. | Ran gofmt/diff checks; `go test ./internal/service -run '^TestResolver' -count=1`; `go test ./...`; `go test -race ./...`; `go vet ./...`; `make check`—all passed. Updated M05 checkbox and source-plan execution log. | `active`, M06 `implementing`; create M06 checklist and dispatch initial `implementer` packet. |
| 2026-08-12 | main agent | Created M06 milestone ledger. | Scope checklist captures M06 requirements, named test slices, exit criterion, and applicable global gates. | `active`, M06 `implementing`; dispatch initial `implementer` packet. |
| 2026-08-12 | main agent | Dispatched initial M06 packet to `implementer`. | Complete packet assigns M06 CLI/render/service/config implementation and tests, requires RED/GREEN evidence for every behavior, and requires all milestone quality gates. | `active`, M06 `implementing`; await complete implementation submission before inspection/review. |
| 2026-08-12 | implementer | Reported public-flag collision before editing. | M05 requires global `--project <path>` while spec §9/§87 requires bare `config set --project <key> <value>` as project scope. This is partial analysis, not a complete submission or remediation attempt. | `active`, M06 `implementing`; main agent selects a compatible command grammar from the authoritative command positions and resumes the initial packet. |
| 2026-08-12 | main agent | Reconciled the M06 CLI flag grammar. | Keep global selector exactly as `wtree --project <path> <command>` (spec §13). Interpret bare `--project` occurring after `config` as the documented project-scope marker (spec §9/§87); its implementation must preprocess only that command-local bare marker before Cobra parses persistent flags. Combined use is `wtree --project <path> config set --project <key> <value>`. This preserves both contracts without a new public spelling. | `active`, M06 `implementing`; implement and test the compatible grammar test-first. |
| 2026-08-12 | implementer | Reported partial M06 progress. | RED→GREEN slices reported for application error taxonomy/CLI mapping, render JSON envelope/writer errors, and initial global config set/get JSON. Remaining scope includes project scopes/combined syntax, unset/list, validation/options, concurrency, CLI broken pipe, integration, and quality gates. | `active`, M06 `implementing`; complete every unchecked scope item. This partial progress is not a submission or an attempt. |
| 2026-08-12 | implementer | Submitted complete initial M06 implementation. | Reported RED→GREEN evidence for error taxonomy, render package, config command, atomic config persistence, JSON error categorization, and JSON boolean spelling. Reported focused suites plus full tests/race/vet/`make check`, gofmt, and diff check passing; no unresolved concerns. | `active`, M06 `implementing`; main agent focused verification passed and the complete shared filesystem proceeds to independent review. |
| 2026-08-12 | main agent | Dispatched M06 independent review to `reviewer`. | Packet requires read-only current-filesystem review of all M06 contracts, test-first evidence, safety/portability/scope, and focused/global verification. | `active`, M06 `reviewing`; await reviewer decision. |
| 2026-08-12 | reviewer | Rejected complete initial M06 submission. | R1: project `Set`/`Unset` mutate before validating required global config (medium, `internal/service/config.go:68,76,79,109–125`). R2: CLI config mutation tests do not isolate XDG/AppData/LocalAppData (medium, `internal/cli/config_test.go:21,91`). Reviewer global/focused gates passed. | `active`, M06 `implementing`; initial-submission rejection does not increment remediation attempts. Send R1+R2 together to normal implementer. |
| 2026-08-12 | main agent | Dispatched complete M06 remediation packet to normal `implementer`. | Packet contains R1 and R2 together, requires new RED→GREEN regressions and all quality gates. Remediation attempts remain `0` until a complete remediation submission is rejected. | `active`, M06 `implementing`; await complete R1+R2 submission. |
| 2026-08-12 | implementer | Submitted complete normal-tier M06 remediation. | R1 regression proves malformed/unsupported global config leaves project config unchanged before failed Set/Unset; R2 regression proves hostile inherited OS config paths are isolated. Reported focused and global tests/race/vet/`make check`/format/diff passing. Main-agent focused verification passed. | `active`, M06 `reviewing`; normal reviewer re-reviews current filesystem. |
| 2026-08-12 | main agent | Dispatched normal M06 remediation re-review. | Read-only packet bounds review to R1/R2 resolution plus material regression checks on the complete current filesystem. | `active`, M06 `reviewing`; await reviewer decision. |
| 2026-08-12 | reviewer | Approved complete M06 remediation. | R1 and R2 resolved; no new material findings; focused/full/race/vet/`make check`/format/diff all passed. | `active`, M06 `reviewing`; main agent verifies and approves. |
| 2026-08-12 | main agent | Verified and approved M06. | Focused R1 and CLI config tests, full tests, race, vet, `make check`, gofmt, and diff check all passed. Updated source-plan checkbox and execution log. | `active`, M07 `implementing`; derive checklist and dispatch initial implementation. |
| 2026-08-12 | main agent | Hardened final-response process controls. | Added mandatory final-response gate, ledger permission field, prohibited false endpoints, pre-final checklist, manual transition traces, and repository-level pointer. `git diff --check` and required trace text passed. | `active`, M07 `implementing`; create M07 checklist and dispatch initial implementation. |
| 2026-08-12 | main agent | Created M07 milestone ledger. | Checklist covers source-plan scope, named test slices, exit criterion, and global quality gates. `Final response permitted` remains `no`. | `active`, M07 `implementing`; dispatch initial `implementer` packet. |
| 2026-08-12 | main agent | Dispatched initial M07 packet to `implementer`. | Complete test-first packet assigns immutable planning/preflight and related tests, with all M07 scope and quality gates. | `active`, M07 `implementing`; await a complete implementation submission before inspection/review. |
| 2026-08-12 | implementer | Reported partial M07 progress. | RED→GREEN evidence reported for immutable plan values, service planner/preflight, and safety regressions; CLI dry-run/mount/path/rendering remains in progress after a RED unknown-`--dry-run` test. | `active`, M07 `implementing`; complete CLI behavior, remaining tests, and all quality gates. This is not a submission or attempt. |
| 2026-08-12 | implementer | Submitted complete initial M07 implementation. | Reported RED→GREEN for plan model, parent-first independent HEAD bases, preflight safety, CLI dry-run rendering, and checkout option rejection; full quality gates passed. Main-agent focused plan/service/CLI verification passed. | `active`, M07 `reviewing`; dispatch normal reviewer. |
| 2026-08-12 | main agent | Dispatched M07 independent review to `reviewer`. | Read-only packet covers full immutable planning/preflight scope, contracts, test quality, scope boundaries, and all verification gates. | `active`, M07 `reviewing`; await reviewer decision. |
| 2026-08-12 | reviewer | Rejected complete initial M07 submission. | R1 (medium): local branch-name validation in `internal/service/plan.go:319` accepts Git-invalid names `.feature`, `feature/`, and `foo/.bar`; CLI dry-run returns success although Git rejects them. Reviewer focused/full/race/vet/`make check`/format/diff gates passed. | `active`, M07 `implementing`; initial-submission rejection does not increment remediation attempts. Send R1 to normal implementer. |
| 2026-08-12 | main agent | Dispatched complete normal-tier M07 remediation packet to `implementer`. | Packet contains R1, requires authoritative Git-compatible validation plus RED→GREEN planner/CLI regressions and all quality gates. Attempts remain `0` until a complete remediation submission is rejected. | `active`, M07 `implementing`; await complete R1 submission. |
| 2026-08-12 | implementer | Submitted complete normal-tier M07 remediation. | Replaced partial branch grammar with Git adapter `check-ref-format --branch`; planner and CLI tests now reject invalid branches without target mutation. Reported full quality gates passed; main-agent focused adapter/planner/CLI verification passed. | `active`, M07 `reviewing`; normal reviewer re-reviews current filesystem. |
| 2026-08-12 | main agent | Dispatched normal M07 remediation re-review. | Packet bounds review to R1’s Git-authoritative validation plus material regression checks on full M07 scope. | `active`, M07 `reviewing`; await reviewer decision. |
| 2026-08-12 | reviewer | Rejected complete normal-tier M07 remediation. | R1 resolved. R2 (medium): `internal/git/adapter.go:93` converts every repository-scoped check-ref-format status 128 into invalid branch, masking unavailable repository operational failures and breaking error taxonomy. | `active`, M07 `implementing`; complete remediation submission rejected, attempts `0 → 1`; dispatch R2 to normal implementer. |
| 2026-08-12 | main agent | Dispatched complete normal-tier M07 R2 remediation packet to `implementer`. | Packet requires Git-authoritative syntax validation without masking unavailable repository/process failure, RED→GREEN adapter/planner regressions, retained R1 behavior, and all quality gates. | `active`, M07 `implementing`; await complete R2 submission. |
| 2026-08-12 | implementer | Submitted complete normal-tier M07 R2 remediation. | `ValidBranchName` now validates Git syntax without `-C`, then verifies valid-name repository reachability via the adapter; adapter/planner/CLI regressions and all quality gates passed. Main-agent focused verification passed. | `active`, M07 `reviewing`; normal reviewer re-reviews current filesystem. |
| 2026-08-12 | main agent | Dispatched normal M07 R2 remediation re-review. | Packet bounds review to R2 operational-error preservation, retained R1 behavior, and material M07 regressions. | `active`, M07 `reviewing`; await reviewer decision. |
| 2026-08-12 | reviewer | Approved complete M07 R2 remediation. | R1/R2 resolved; Git validates syntax independently, unavailable repositories preserve Git errors, and named invalid/valid branch behavior is correct. | `active`, M07 `reviewing`; main agent verifies and approves. |
| 2026-08-12 | main agent | Verified and approved M07. | Focused plan/preflight/CLI tests, full tests/race/vet/`make check`, gofmt, and diff check passed. Updated source-plan checkbox and execution log. | `active`, M08 `implementing`; create M08 checklist and dispatch initial implementation. |
| 2026-08-12 | main agent | Created M08 milestone ledger. | Checklist covers transaction scope, named test slices, exit criterion, and applicable global gates. Final-response permission remains `no`. | `active`, M08 `implementing`; dispatch initial `implementer` packet. |
| 2026-08-12 | main agent | Dispatched initial M08 packet to `implementer`. | Complete packet assigns test-first transaction/rollback/recovery implementation and all M08 verification gates. | `active`, M08 `implementing`; await complete submission before inspection/review. |
| 2026-08-12 | implementer | Submitted complete initial M08 implementation. | Reported RED→GREEN runner/coordinator/failure-boundary/cancellation/cleanup-recovery slices, complete scope coverage, and full quality gates. Main-agent focused transaction/service verification passed. | `active`, M08 `reviewing`; dispatch normal reviewer. |
| 2026-08-12 | main agent | Dispatched M08 independent review to `reviewer`. | Packet requires read-only full M08 safety, transaction, recovery, test-quality, and quality-gate review. | `active`, M08 `reviewing`; await reviewer decision. |
| 2026-08-12 | reviewer | Rejected complete initial M08 submission. | R1 high state source-of-truth path divergence; R2 high deletion of prior state on failure; R3 medium optional required validation boundaries; R4 medium non-actionable mixed-rollback recovery metadata. | `active`, M08 `implementing`; initial rejection uncounted; dispatch complete R1–R4 normal-tier remediation. |
| 2026-08-12 | main agent | Hardened durable-ledger layout instructions. | Added `docs/ai/run-ledger-layout.md`; referenced it from `AGENTS.md` and milestone supervision. It mandates one ledger shape, active-state invariants, exact checkpoint content, transition ordering, resume and pre-final audits, and manual traces. `git diff --check` and required layout-trace text passed. | `active`, M08 `implementing`; await complete R1–R4 remediation submission. |
| 2026-08-12 | implementer | Submitted complete normal-tier M08 R1–R4 remediation. | Shared established state-path helper now drives init/planner/resolver/transaction; prior state is restored byte-for-byte; revalidation/result validation are mandatory; recovery records unreverted mixed-rollback effects and failures. Reported RED→GREEN regressions and full quality gates. Main-agent focused store/transaction/service verification passed. | `active`, M08 `reviewing`; dispatch normal reviewer. |
| 2026-08-12 | main agent | Dispatched normal M08 R1–R4 remediation re-review. | Read-only packet bounds review to R1–R4 resolution and material transaction-safety regressions on the current filesystem. | `active`, M08 `reviewing`; await reviewer decision. |
| 2026-08-12 | reviewer | Rejected complete normal-tier M08 R1–R4 remediation. | R1–R4 resolved. R5 high: `internal/service/transaction.go:211` removes pre-existing recovery metadata after a clean later rollback, losing the earlier recovery description. | `active`, M08 `implementing`; complete remediation rejected, attempts `0 → 1`; dispatch R5 normal-tier remediation. |
| 2026-08-12 | main agent | Dispatched complete normal-tier M08 R5 remediation packet to `implementer`. | Packet requires pre-existing recovery preservation (or safe preflight rejection), RED→GREEN regression, retained current-attempt cleanup semantics, and all quality gates. | `active`, M08 `implementing`; await complete R5 submission. |
| 2026-08-12 | implementer | Submitted complete normal-tier M08 R5 remediation. | Project lock now detects a pre-existing recovery record and returns conflict before effects; a current-attempt record still clears on clean rollback. RED→GREEN regressions and full quality gates passed. Main-agent focused verification passed. | `active`, M08 `reviewing`; normal reviewer re-reviews current filesystem. |
| 2026-08-12 | main agent | Dispatched normal M08 R5 remediation re-review. | Packet bounds review to R5 recovery preservation/current-attempt cleanup and material M08 safety regressions. | `active`, M08 `reviewing`; await reviewer decision. |
| 2026-08-12 | reviewer | Approved complete M08 R5 remediation. | R5 resolved; pre-existing recovery is protected under lock, current-attempt cleanup remains correct, and R1–R4 behavior holds. | `active`, M08 `reviewing`; main agent verifies and approves. |
| 2026-08-12 | main agent | Verified and approved M08. | Focused transaction/service tests, full tests/race/vet/`make check`, and diff check passed. Updated source-plan checkbox and execution log. | `active`, M09 `implementing`; create M09 checklist and dispatch initial implementation. |
| 2026-08-12 | main agent | Created M09 milestone ledger. | Current snapshot now contains M09-only scope, `normal` tier, `0` attempts, no findings, and final-response permission `no`. | `active`, M09 `implementing`; dispatch initial `implementer` packet. |
| 2026-08-12 | main agent | Dispatched initial M09 packet to `implementer`. | Complete test-first packet assigns create execution/options/output integration and M09 verification gates. | `active`, M09 `implementing`; await complete submission before inspection/review. |
| 2026-08-12 | main agent | Reconciled interruption and complete M09 submission. | The implementer completed M09 during the erroneous final-response gap. Main agent inspected current worktree, `git diff --check`, and focused `TestWorkspaceCreator`/`TestExecuteCreate` suites; all passed. | `active`, M09 `reviewing`; dispatch normal independent review. |
| 2026-08-12 | reviewer | Rejected complete initial M09 submission. | R1 high cancellation context missing; R2 medium rollback-result output missing; R3 medium concurrent create integration coverage missing. | `active`, M09 `implementing`; initial rejection uncounted; dispatch complete R1–R3 remediation. |
| 2026-08-12 | main agent | Reconciled interrupted R1–R3 remediation. | Implementer completed signal-aware context propagation/cancellation rollback, rollback-result rendering, and concurrent create tests while the prior turn ended incorrectly. Main-agent focused CLI/render/service checks and diff/format checks passed. | `active`, M09 `reviewing`; dispatch normal re-review. |
| 2026-08-12 | reviewer | Rejected complete normal-tier M09 R1–R3 remediation. | R1–R3 resolved. R4 medium: no non-dry-run root→backend→shared create exercising renamed `backend=api`, `shared=common` and persisted state. | `active`, M09 `implementing`; complete remediation rejected, attempts `0 → 1`; dispatch R4 normal-tier remediation. |
| 2026-08-12 | implementer | Submitted complete normal-tier M09 R4 remediation. | Added non-dry-run three-level renamed workspace and grandchild-effect-boundary rollback integration tests. The tests passed immediately because existing M09 behavior was correct; no production change was warranted. Full quality gates passed. | `active`, M09 `reviewing`; main-agent focused three-level/rollback test and format/diff checks passed; dispatch normal re-review. |
| 2026-08-12 | reviewer | Approved complete M09 R4 remediation. | R4 resolved; actual three-level renamed paths/identities/branches/state and grandchild rollback are covered; R1–R3 remain resolved. | `active`, M09 `reviewing`; main agent verifies and approves. |
| 2026-08-12 | main agent | Verified and approved M09. | Focused create/rollback/concurrency/CLI-render tests, full tests/race/vet/`make check`, and diff check passed. Updated source-plan checkbox and execution log. | `active`, M10 `implementing`; create M10 checklist and dispatch initial implementation. |
| 2026-08-12 | main agent | Created M10 milestone ledger. | Current snapshot contains M10-only scope, normal tier, zero attempts, no findings, and non-terminal response permission. | `active`, M10 `implementing`; dispatch initial `implementer` packet. |
| 2026-08-12 | main agent | Dispatched initial M10 packet to `implementer`. | Complete test-first packet assigns checkout/restoration and inspection lookup implementation with M10 quality gates. | `active`, M10 `implementing`; await complete submission. |
| 2026-08-12 | implementer | Submitted complete initial M10 implementation. | Added existing-branch checkout/retained-state restoration, list/path/repo lookup, partial state persistence, and contract tests; reported full quality gates. Main-agent named CLI/service/store verification and diff/format checks passed. | `active`, M10 `reviewing`; dispatch normal reviewer. |
| 2026-08-12 | reviewer | Rejected complete initial M10 submission. | R1 retained mount overlay failure; R2 missing workspace in repo-get JSON; R3 missing partial/detached/incompatible checkout refusal regression. | `active`, M10 `implementing`; initial rejection uncounted; dispatch complete R1–R3 remediation. |
| 2026-08-12 | implementer | Submitted complete normal-tier M10 R1–R3 remediation. | Retained mounts now overlay, repo-get JSON has workspace object, and persisted partial/detached/divergent state refuses without mutation. Reported full gates; main-agent focused CLI suite and diff/format checks passed. | `active`, M10 `reviewing`; dispatch normal re-review. |
| 2026-08-12 | reviewer | Rejected complete normal-tier M10 R1–R3 remediation. | R1/R3 resolved. R2 remains: spec §28 requires `workspace` to be logical name string, not object. | `active`, M10 `implementing`; complete remediation rejected, attempts `0 → 1`; dispatch R2 normal-tier remediation. |
| 2026-08-12 | implementer | Submitted complete normal-tier M10 R2 remediation. | Repo-get JSON `workspace` is now exact logical-name string; RED→GREEN structural test and full quality gates passed. Main-agent focused verification passed. | `active`, M10 `reviewing`; dispatch normal re-review. |
| 2026-08-12 | reviewer | Approved complete M10 R2 remediation. | R2 exact workspace string contract holds; R1/R3 remain correct. | `active`, M10 `reviewing`; main agent verifies and approves. |
| 2026-08-12 | main agent | Verified and approved M10. | Focused checkout/lookup safety tests, full tests/race/vet/`make check`, and diff check passed. Updated source-plan checkbox/execution log. | `active`, M11 `implementing`; create M11 checklist and dispatch initial implementation. |
| 2026-08-12 | main agent | Created M11 milestone ledger. | Current snapshot contains M11-only scope, normal tier, zero attempts, no findings, and non-terminal response permission. | `active`, M11 `implementing`; dispatch initial `implementer` packet. |
| 2026-08-12 | main agent | Reconciled premature stop after M11 dispatch. | M11 initial packet had been dispatched to `implementer` but its checkpoint was omitted when the prior turn ended incorrectly; no implementation completion is inferred. | `active`, M11 `implementing`; await complete initial submission. |
| 2026-08-12 | implementer | Submitted complete initial M11 implementation. | Added read-only aggregate/per-repo status, drift reconciliation, status CLI/rendering, optional upstream facts, and matrix tests; reported RED→GREEN and full gates. Main-agent focused status/CLI/Git verification plus format/diff checks passed. | `active`, M11 `reviewing`; dispatch normal reviewer. |
| 2026-08-12 | reviewer | Rejected complete initial M11 submission. | R1 status can refresh Git index because optional locks remain enabled; R2 quoted porcelain path display breaks filtering of managed children with spaces/Unicode. | `active`, M11 `implementing`; initial rejection uncounted; dispatch complete R1–R2 remediation. |
| 2026-08-12 | implementer | Submitted complete normal-tier M11 R1–R2 remediation. | Fact-only Git uses optional locks disabled; NUL porcelain parser preserves spaces/Unicode and rename paths; status filtering and index metadata regressions pass. Main-agent focused Git/service verification plus diff/format checks passed. | `active`, M11 `reviewing`; dispatch normal re-review. |
| 2026-08-12 | reviewer | Rejected complete normal-tier M11 R1–R2 remediation. | R1/R2 resolved. R3 medium: an outside-to-managed-child rename loses its source path and is incorrectly filtered clean. | `active`, M11 `implementing`; complete remediation rejected, attempts `0 → 1`; dispatch R3 normal-tier remediation. |
| 2026-08-12 | implementer | Submitted complete normal-tier M11 R3 remediation. | NUL rename/copy parser preserves original path; filtering suppresses only changes wholly within managed child. Real staged outside→child rename regression and full gates passed. Main-agent focused Git/service verification plus diff/format checks passed. | `active`, M11 `reviewing`; dispatch normal re-review. |
| 2026-08-12 | reviewer | Approved complete M11 R3 remediation. | R3 correct and R1/R2 remain correct; all M11 status contracts hold. | `active`, M11 `reviewing`; main agent verifies and approves. |
| 2026-08-12 | main agent | Verified and approved M11. | Focused status/Git parser/read-only tests, full tests/race/vet/`make check`, and diff check passed. Updated source-plan checkbox/execution log. | `active`, M12 `implementing`; create M12 checklist and dispatch initial implementation. |
| 2026-08-12 | main agent | Created M12 milestone ledger. | Current snapshot contains M12-only scope, normal tier, zero attempts, no findings, and non-terminal response permission. | `active`, M12 `implementing`; dispatch initial `implementer` packet. |
| 2026-08-12 | implementer | Submitted complete initial M12 implementation. | Added child-first remove with retained state/branches, dirty-force policy, no-repair preflight, locking/rollback/recovery and CLI contracts; reported RED→GREEN/full gates. Main-agent focused service/CLI and diff/format verification passed. | `active`, M12 `reviewing`; dispatch normal reviewer. |
| 2026-08-12 | reviewer | Rejected complete initial M12 submission. | R1 high: forced dirty nested removal uses an impossible `AddWorktree` inverse; a later failure can lose dirty data yet report clean rollback/no recovery. | `active`, M12 `implementing`; initial rejection uncounted; dispatch complete R1 remediation. |
| 2026-08-12 | implementer | Submitted complete normal-tier M12 R1 remediation. | Forced dirty removal is non-reversible; a later removal failure produces rollback-incomplete recovery identifying dirty removal and failed later step. Regression and full gates passed. Main-agent focused service/CLI and diff/format verification passed. | `active`, M12 `reviewing`; dispatch normal re-review. |
| 2026-08-12 | reviewer | Approved complete M12 R1 remediation. | Forced dirty failures correctly produce actionable recovery; clean and successful force behavior remains correct. | `active`, M12 `reviewing`; main agent verifies and approves. |
| 2026-08-12 | main agent | Verified and approved M12. | Focused forced-dirty recovery, full tests/race/vet/`make check`, and diff check passed. Updated source plan. | `active`, M13 `implementing`; create M13 checklist and dispatch implementation. |
| 2026-08-12 | main agent | Dispatched initial M13 packet to `implementer`. | Complete test-first delete packet covers conservative worktree/branch/state deletion and M13 quality gates. | `active`, M13 `implementing`; await complete submission. |
| 2026-08-12 | implementer | Submitted complete initial M13 implementation. | Added conservative delete with merged-branch preflight, scoped force, ordered removal/branch/state changes, and recovery; reported RED→GREEN/full gates. Main-agent focused Git/service/CLI and diff/format verification passed. | `active`, M13 `reviewing`; dispatch normal reviewer. |
| 2026-08-12 | reviewer | Rejected complete initial M13 submission. | R1 high: unmerged override incorrectly makes clean removal non-reversible. R2 high: global force widens dirty and branch force allowances to unrelated effects, enabling unreported TOCTOU data loss. | `active`, M13 `implementing`; initial rejection uncounted; dispatch complete R1–R2 remediation. |
| 2026-08-12 | implementer | Submitted complete normal-tier M13 R1–R2 remediation. | Typed immutable worktree/branch allowances now scope Git force flags exactly; clean unmerged child restores after later failure. Dirty-only/unmerged-only/combined and rollback regressions/full gates passed. Main-agent focused service/CLI and diff/format verification passed. | `active`, M13 `reviewing`; dispatch normal re-review. |
| 2026-08-12 | reviewer | Approved complete M13 R1–R2 remediation. | Force allowances are exact, clean unmerged rollback remains reversible, and no scope widening remains. | `active`, M13 `reviewing`; main agent verifies and approves. |
| 2026-08-12 | main agent | Verified and approved M13. | Focused force-scope tests, full tests/race/vet/`make check`, and diff check passed. Updated source plan. | `active`, M14 `implementing`; create M14 checklist and dispatch implementation. |
| 2026-08-12 | main agent | Created M14 milestone ledger. | Current snapshot contains M14-only scope, normal tier, zero attempts, no findings, and non-terminal response permission. | `active`, M14 `implementing`; dispatch initial `implementer` packet. |
| 2026-08-12 | main agent | Dispatched initial M14 packet to `implementer`. | Complete test-first import packet covers external discovery, identity/mount mapping, partial semantics, atomic persistence, and M14 quality gates. | `active`, M14 `implementing`; await complete submission. |
| 2026-08-12 | implementer | Submitted complete initial M14 implementation. | Added identity-based external import, observed mount/branch/head persistence, partial and collision semantics, and CLI contracts; reported RED→GREEN/full gates. Main-agent focused service/CLI and diff/format verification passed. | `active`, M14 `reviewing`; dispatch normal reviewer. |
| 2026-08-12 | reviewer | Rejected complete initial M14 submission. | R1 ambiguous divergent/detached import name needs `--name`; R2 dry-run can reconcile/write moved-project registry. | `active`, M14 `implementing`; initial rejection uncounted; dispatch complete R1–R2 remediation. |
| 2026-08-12 | implementer | Submitted complete normal-tier M14 R1–R2 remediation. | Divergent/detached multi-checkout imports require explicit name; project resolution is read-only and non-dry-run reconciles only after preflight. Regressions/full gates passed. Main-agent focused service/CLI and diff/format verification passed. | `active`, M14 `reviewing`; dispatch normal re-review. |
| 2026-08-12 | reviewer | Approved complete M14 R1–R2 remediation. | Ambiguous topology naming and dry-run registry immutability are correct; existing import contracts remain intact. | `active`, M14 `reviewing`; main agent verifies and approves. |
| 2026-08-12 | main agent | Verified and approved M14. | Focused import name/dry-run tests, full tests/race/vet/`make check`, and diff check passed. Updated source plan. | `active`, M15 `implementing`; create M15 checklist and dispatch implementation. |
| 2026-08-12 | main agent | Created M15 milestone ledger. | Current snapshot contains M15-only scope, normal tier, zero attempts, no findings, and non-terminal response permission. | `active`, M15 `implementing`; dispatch initial `implementer` packet. |
| 2026-08-12 | main agent | Dispatched initial M15 packet to `implementer`. | Complete test-first doctor packet covers diagnostics, safe repairs, recovery, and M15 verification gates. | `active`, M15 `implementing`; await complete submission. |
| 2026-08-12 | implementer | Reported M15 contract uncertainty before editing. | Spec uses broad diagnostic language, but this is analysis only—not a submission, finding, or blocker. | `active`, M15 `implementing`; main agent applies the plan's stated safe-fix allowlist as the bounded initial implementation contract. |
| 2026-08-12 | main agent | Bounded M15 implementation from approved plan text. | Initial M15 implements read-only findings with stable existing error/JSON conventions; `--fix` permits only verified stale mount/path metadata, safely removable stale state, and narrowly scoped Git worktree prune/repair. All other findings remain diagnostics, never automatic repairs. | `active`, M15 `implementing`; resume initial test-first packet. |
| 2026-08-12 | implementer | Submitted complete initial M15 implementation. | Added doctor diagnostics and bounded `--fix` allowlist with project lock/atomic writes/dry-run contracts; reported RED→GREEN and full gates. Main-agent focused doctor CLI/service and diff/format verification passed. | `active`, M15 `reviewing`; dispatch normal reviewer. |
| 2026-08-12 | reviewer | Rejected complete initial M15 submission. | R1 high: remove-stale-state can erase valid M12 retained/recovery state. R2 medium: doctor/default dry-run resolution can mutate registry. | `active`, M15 `implementing`; initial rejection uncounted; dispatch complete R1–R2 remediation. |
| 2026-08-12 | implementer | Submitted complete normal-tier M15 R1–R2 remediation. | Removed unsafe stale-state deletion from fix allowlist; retained/recovery state stays diagnostic. Doctor/dry-run use read-only resolver and preserve stale registry bytes. Regressions/full gates passed. Main-agent focused verification plus diff/format checks passed. | `active`, M15 `reviewing`; dispatch normal re-review. |
| 2026-08-12 | reviewer | Approved complete M15 R1–R2 remediation. | Doctor preserves retained/recovery state and plain/dry-run resolution is registry-read-only; safe allowlist remains narrow. | `active`, M15 `reviewing`; main agent verifies and approves. |
| 2026-08-12 | main agent | Verified and approved M15. | Focused doctor tests, full tests/race/vet/`make check`, and diff check passed. Updated source plan. | `active`, M16 `implementing`; create M16 checklist and dispatch implementation. |
| 2026-08-12 | main agent | Created M16 milestone ledger. | Current snapshot contains M16-only scope, normal tier, zero attempts, no findings, and non-terminal response permission. | `active`, M16 `implementing`; dispatch initial `implementer` packet. |
| 2026-08-12 | main agent | Dispatched initial M16 packet to `implementer`. | Complete test-first UX packet covers help, how-to, option compatibility, redaction, error context, and M16 quality gates. | `active`, M16 `implementing`; await complete submission. |
| 2026-08-12 | implementer | Submitted complete initial M16 implementation. | Added comprehensive command help, 24-topic global how-to plus command guides, option-matrix documentation/tests, and verbose environment-redaction regression; reported full gates. Main-agent focused help/how-to/redaction and diff/format verification passed. | `active`, M16 `reviewing`; dispatch normal reviewer. |
| 2026-08-12 | reviewer | Rejected complete initial M16 submission. | R1 raw how-to bypasses invocation validation; R2 doctor dry-run without fix is silently ignored. | `active`, M16 `implementing`; initial rejection uncounted; dispatch complete R1–R2 remediation. |
| 2026-08-12 | implementer | Submitted complete normal-tier M16 R1–R2 remediation. | How-to is validated terminal root/supported-command syntax; invalid invocations use exit 2/JSON envelope. Doctor dry-run requires fix. Regressions/full gates passed. Main-agent focused verification plus diff/format checks passed. | `active`, M16 `reviewing`; dispatch normal re-review. |
| 2026-08-12 | reviewer | Approved complete M16 R1–R2 remediation. | How-to forms validate strictly; doctor dry-run compatibility and broader UX contracts remain correct. | `active`, M16 `reviewing`; main agent verifies and approves. |
| 2026-08-12 | main agent | Verified and approved M16. | Focused how-to/option tests, full tests/race/vet/`make check`, and diff check passed. Updated source plan. | `active`, M17 `implementing`; create M17 checklist and dispatch implementation. |
| 2026-08-12 | main agent | Created M17 milestone ledger. | Current snapshot contains M17-only scope, normal tier, zero attempts, no findings, and non-terminal response permission. | `active`, M17 `implementing`; dispatch initial `implementer` packet. |
| 2026-08-12 | main agent | Dispatched initial M17 packet to `implementer`. | Complete test-first release-readiness packet covers packaging, cross-platform hardening, black-box/fuzz tests, invariant audit, and M17 quality gates. | `active`, M17 `implementing`; await complete submission. |
| 2026-08-12 | implementer | Reported M17 release-contract uncertainty before editing. | Existing repository has no license/notice or established artifact/version schema. This is analysis only, not a submission or blocker decision. | `active`, M17 `implementing`; main agent derives repository-native non-legal build/testing contract and isolates license choice as pending external authority. |
| 2026-08-12 | main agent | Bounded non-legal M17 packet and identified license authority gap. | Use reproducible `dist/wtree_<version>_<os>_<arch>[.exe]` artifacts and SHA-256 manifest; retain no-publish policy; extend existing CI. Do not invent a license or legal notice where no license policy exists. | `active`, M17 `implementing`; implement non-legal release hardening while awaiting user license direction before M17 can be approved. |
| 2026-08-12 | user / main agent | Resolved M17 legal release authority. | User authorized MIT License, copyright holder Define Business LTD, and author attribution Marcel Linnenfelser with Codex. Main agent added `LICENSE` and `NOTICE`. | `active`, M17 `implementing`; incorporate authorized legal artifacts into the complete M17 packet and continue implementation. |
| 2026-08-12 | implementer | Submitted partial M17 non-legal progress. | Completed packaging, CI release-layout checks, installation docs, E2E lifecycle/import, fuzz targets, and traceability draft; preserved no-publish rule. Legal and CLI-level rollback/recovery E2E remained unfinished, so this is not a complete submission or review candidate. | `active`, M17 `implementing`; complete legal release documentation and required recovery-flow evidence. |
| 2026-08-12 | main agent | Returned remaining M17 scope to `implementer`. | License authority is now resolved. Packet requires MIT/NOTICE release documentation and required rollback/recovery black-box evidence or concrete safely testable coverage. | `active`, M17 `implementing`; await complete M17 submission. |
| 2026-08-12 | implementer | Submitted complete M17 release-readiness implementation. | Added deterministic cross-platform release layout/manifest/legal artifacts, CI checks, install docs, lifecycle/import/recovery CLI E2E, fuzz targets, and traceability. No publishing/signing/tagging/uploads occurred; reported full quality gates. | `active`, M17 `implementing`; main agent validates release layout and prepares normal review. |
| 2026-08-12 | main agent | Validated M17 release layout hygiene. | Ran `make release VERSION=0.0.0-test`; required artifacts, legal files, and manifest were produced; E2E recovery/lifecycle/import suite and diff check passed. Added `/dist/` to `.gitignore` so local artifacts are not tracked. Environment rejects deletion commands, so the known ignored test output remains locally. | `active`, M17 `reviewing`; dispatch normal reviewer. |
| 2026-08-12 | reviewer | Rejected complete M17 submission. | R1 reused output non-convergent; R2 store/storage fuzzers absent; R3 nested import and organic rollback/recovery E2E absent; R4 fuzz files unformatted so `make check` fails. | `active`, M17 `implementing`; initial rejection uncounted; dispatch complete R1–R4 remediation. |
| 2026-08-12 | implementer | Submitted complete normal-tier M17 R1–R4 remediation. | Release staging safely converges owned artifacts and preserves unrelated output; store/storage fuzzers added; nested identity import and organic CLI rollback-incomplete recovery E2E added; fuzz formatting fixed. Full gates passed. Main-agent release-test/E2E/separate fuzz/format/diff verification passed. | `active`, M17 `reviewing`; dispatch normal re-review. |
| 2026-08-12 | reviewer | Rejected complete normal-tier M17 R1–R4 remediation. | R1–R4 otherwise resolved. R5 medium broad `wtree_*` cleanup deletes unrelated prefix files. R6 medium POSIX Git shim is not Windows CI portable. | `active`, M17 `implementing`; complete remediation rejected, attempts `0 → 1`; dispatch R5–R6 normal-tier remediation. |
| 2026-08-12 | implementer | Submitted complete normal-tier M17 R5–R6 remediation. | Cleanup now matches only valid artifact schema and preserves prefix collisions; recovery E2E uses compiled native Git helper with `.exe` on Windows. Release regression/E2E/full gates passed. Main-agent focused release-test/E2E/diff/format verification passed. | `active`, M17 `reviewing`; dispatch normal re-review. |
| 2026-08-12 | reviewer | Approved complete M17 R5–R6 remediation. | Exact cleanup, native cross-platform recovery helper, legal/release/E2E/fuzz/CI contracts all pass. | `active`, M17 `reviewing`; main agent verifies and approves. |
| 2026-08-12 | main agent | Verified and approved M17. | Release reuse, E2E, fuzz smokes, full tests/race/vet/`make check`, install, and diff check passed. Updated source plan. | `active`, M18 `reviewing`; create acceptance checklist and dispatch clean-room reviewer. |
| 2026-08-12 | main agent | Created M18 acceptance ledger. | Current snapshot contains clean-room acceptance scope, normal tier, zero attempts, no findings, and non-terminal response permission. | `active`, M18 `reviewing`; dispatch `reviewer`. |
| 2026-08-12 | main agent | Dispatched M18 clean-room acceptance review to `reviewer`. | Packet requires specification-first repository acceptance, all release-quality gates, full command/contract/invariant/doc checks, and complete severity-backed findings. | `active`, M18 `reviewing`; await reviewer decision. |
| 2026-08-12 | reviewer | Rejected M18 clean-room acceptance. | R1 read-only/dry-run registry mutation; R2 JSON code/exit taxonomy mismatch; R3 ignored init `--project`; R4 invalid nested help/hidden flags; R5 inaccurate non-per-section traceability. | `active`, M18 `implementing`; initial acceptance rejection uncounted; dispatch complete R1–R5 remediation. |
| 2026-08-12 | implementer | Submitted complete normal-tier M18 R1–R5 remediation. | Inspection/dry-run resolver is read-only, JSON/exit classification unified, init selector rejected, nested help corrected, and traceability replaced with §§1–93 matrix. Full gates passed. Main-agent focused CLI/process-boundary/traceability/diff verification passed. | `active`, M18 `reviewing`; dispatch clean-room re-review. |
| 2026-08-12 | reviewer | Rejected complete normal-tier M18 R1–R5 remediation. | R1/R2/R3/R5 resolved. R4 remains: generated init/config child help advertises generic inherited `--project <path>` despite init rejection and config's command-local bare scope-marker overload. | `active`, M18 `implementing`; complete remediation rejected, attempts `0 → 1`; dispatch normal-tier R4 remediation. |
| 2026-08-12 | main agent | Dispatched normal-tier M18 R4 remediation to `implementer`. | Packet limits ownership to CLI help rendering and semantic help-contract tests; it requires accurate init/config `--project` behavior and executable documented forms. | `active`, M18 `implementing`; await complete R4 submission, then independently re-review the shared filesystem. |
| 2026-08-12 | implementer | Submitted complete normal-tier M18 R4 remediation. | Init child help suppresses the unsupported inherited selector; config help hides that ambiguous inherited selector and documents root selection, bare project scope, and their valid combined form. A semantic test executes the combined form. RED/GREEN, focused help suite, full/race/vet/`make check`/format/diff checks passed. | `active`, M18 `reviewing`; main agent focused help-contract test and diff check passed; dispatch clean-room re-review. |
| 2026-08-12 | main agent | Dispatched clean-room M18 R4 re-review to `reviewer`. | Read-only packet requires full acceptance regression review, with semantic validation of generated init/config help and previously resolved R1–R5 contracts. | `active`, M18 `reviewing`; await reviewer decision. |
| 2026-08-12 | reviewer | Approved complete normal-tier M18 R4 remediation. | No material findings. Semantically verified init/config selector help and execution, retained R1–R5 fixes, full E2E/fuzz/release/help/CI/legal traceability checks, and all quality gates. | `active`, M18 `verifying`; main agent runs terminal repository gates before approval. |
| 2026-08-12 | main agent | Verified and approved M18; completed the authorized plan run. | `go test ./...`, `go test -race ./...`, `go vet ./...`, `make check`, `go install ./cmd/wtree`, gofmt check, and `git diff --check` all passed. Updated the M18 source-plan checkbox and execution log after clean-room approval. | `complete`, M18 `approved`; final response is permitted. |
